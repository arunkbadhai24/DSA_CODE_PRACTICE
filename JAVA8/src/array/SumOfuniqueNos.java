package array;

import java.util.Arrays;
import java.util.Map;
import java.util.function.Function;
import java.util.stream.Collectors;

public class SumOfuniqueNos {

    static int[] arr ={10,11,21,45,23,95,100,10,21,11,45};
    public static void main(String[] armgs) {
//      Map<Integer,Long> countNums=
//              Arrays.stream(arr).boxed().collect(Collectors.groupingBy(Function.identity(),Collectors.counting()))
//                      .entrySet().stream().filter(x->x.getValue()==1).;
//
//        System.out.println(countNums);
    }
}
